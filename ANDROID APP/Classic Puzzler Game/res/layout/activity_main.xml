<?xml version="1.0" encoding="UTF-8"?>
<LinearLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/Black"
    tools:context="com.tetris.presentation.MainActivity" >
    
    <LinearLayout
    	android:id="@+id/layoutPublicity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="50dp"
        android:orientation="vertical" >
    </LinearLayout>
    
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/background"
        android:gravity="center"
        android:orientation="vertical" >

        <Button
            android:id="@+id/buttonStart"
            style="@style/buttonMainActivity"
            android:text="@string/buttonStart" />

        <Button
            android:id="@+id/buttonRating"
            style="@style/buttonMainActivity"
            android:text="@string/buttonRating" />

        <Button
            android:id="@+id/buttonOptions"
            style="@style/buttonMainActivity"
            android:text="@string/buttonOptions" />
    </LinearLayout>

</LinearLayout>